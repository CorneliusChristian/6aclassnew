#include <iostream>
#include <vector>
int main()
{
    std::vector k = int{2, 3, 5, 7};
    long j = 8;
    for (int i = 0; i < 1000; i++)
    {
        bool isPrimeNumber = true;
        for (long r : k)
        {
            if (j % r == 0)
            {
                isPrimeNumber = false;
                break;
            }
        }
        if (isPrimeNumber)
        {
            std::cout << j << " is a prime number\n";
            k.
        }
        j++;
    }
    int m = 0;
    for(long l : k){
        m++;
        std::cout << l << " ";
        if(m > 15){
            std::cout << "\n";
            m = 0;
        }
    }
}